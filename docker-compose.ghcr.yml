services:
  seerr:
    image: ghcr.io/CHANGE_ME_OWNER/CHANGE_ME_REPO:latest
    container_name: seerr-episode-search
    restart: unless-stopped
    ports:
      - "9715:5055"
    environment:
      - LOG_LEVEL=info
      - TZ=UTC
    volumes:
      - ./config:/app/config
