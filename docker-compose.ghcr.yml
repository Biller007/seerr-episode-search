services:
  seerr:
    image: ghcr.io/biller007/seerr-episode-search:latest
    container_name: seerr-episode-search
    restart: unless-stopped
    ports:
      - "9715:5055"
    environment:
      - LOG_LEVEL=info
      - TZ=UTC
    volumes:
      - ./config:/app/config
